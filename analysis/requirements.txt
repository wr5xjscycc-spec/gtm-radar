# P4 analysis service — test + core stats deps.
# CI installs these; heavy Bayesian deps are added when Phase 4 lands (kept out of CI base to stay fast).
pytest>=8.0
numpy>=1.26
pandas>=2.2
statsmodels>=0.14
# Phase 0 — HTTP service (a Convex action calls this over HTTP) + its test client.
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
# Phase 1 — pandas row assembly + scikit-learn baseline yardstick (not shipped).
scikit-learn>=1.4
# Phase 4 (Bayesian hypothesis generator) — uncomment when implemented:
# pymc>=5.10
# Phase 5 (DiD) also uses:
# linearmodels>=6.0
